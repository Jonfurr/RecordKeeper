<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-md-4">
  </div>
  <div class="col-md-4">
  </div>
  <div class="col-md-4">
  </div>
</div>
<div class="row">
<div class= "col-md-12">
      <table class="table table-striped">
        <thead>
         <!--  <tr><h3 style="text-align: center">Most Recent Albums</h3>
          </tr> -->
          <tr>
            <th>Cover</th>
            <th>Artist</th>
            <th>Album</th>
            <th>Year</th>
           <!--  <th>Label</th>
            <th>Genre</th>
            <th>Format</th>
            <th>Pressing</th> -->
          </tr>
        </thead>

  <tbody>
    <% @albums.each do |album| %>
      <tr>
        <td><a href="/albums/<%= album.id %>"><img class="img-thumbnail" width="100px" src="<%= album.image_url %>"></a></td>
        <td><a href="/albums/<%= album.id %>"><%= album.artist %></a></td>
        <td><a href="/albums/<%= album.id %>"><%= album.title %></a></td>
        <td><a href="/albums/<%= album.id %>"><%= album.year %></a></td>
       <!--  <td><%= album.label %></td>
        <td><%= album.genre %></td>
        <td><%= album.pressing %></td>
        <td><%= album.country %></td>
        <td><%= album.comment %></td> -->
        <!-- <td><%= link_to 'Show', album %></td>
        <td><%= link_to 'Edit', edit_album_path(album) %></td>
        <td><%= link_to 'Destroy', album, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>
</div>

