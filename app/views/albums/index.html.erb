<p id="notice"><%= notice %></p>

<h1>Listing Albums</h1>

<div id="prof_content" class= "col-md-12">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Cover</th>
            <th>Arist</th>
            <th>Album</th>
            <th>Year</th>
           <!--  <th>Label</th>
            <th>Genre</th>
            <th>Format</th>
            <th>Pressing</th> -->
          </tr>
        </thead>
  <tbody>
    <% @albums.each do |album| %>
      <tr>
        <td><img class="img-thumbnail" width="100px" src="<%= album.image_url %>"></td>
        <td><%= album.artist %></td>
        <td><%= album.title %></td>
        <td><%= album.year %></td>
       <!--  <td><%= album.label %></td>
        <td><%= album.genre %></td>
        <td><%= album.pressing %></td>
        <td><%= album.country %></td>
        <td><%= album.comment %></td> -->
        <!-- <td><%= link_to 'Show', album %></td>
        <td><%= link_to 'Edit', edit_album_path(album) %></td>
        <td><%= link_to 'Destroy', album, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Album', new_album_path %>
